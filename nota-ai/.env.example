# Copy to .env if you want build-time Supabase keys.
# Runtime API keys (Deepgram, Anthropic, Supabase) are entered
# in the app's Settings screen and stored in AsyncStorage.
EXPO_PUBLIC_SUPABASE_URL=
EXPO_PUBLIC_SUPABASE_ANON_KEY=
